\section*{Prologue}

\paragraph{P.A1.}
Use the two identities in the definition of linearity and the fact that
averaging is built from addition and scalar multiplication.

\paragraph{P.B1.}
Remember that the grid is circular, so cell $1$ has neighbors $4$ and $2$.

\paragraph{P.D1.}
Try a constant vector first.
